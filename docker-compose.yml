version: '3'
services:
  app:
    build: .
    image: maayanlab/lncrnafp:0.1.1
    environment:
      - ROOT_PATH=/lncrnafp/
      - BASE_PATH=https://maayanalab.cloud
      - DEBUG=true
    ports:
      - 8080:80
